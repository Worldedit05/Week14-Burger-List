/*
ORM that takes inputs and converts them to mysql queries
*/

var connection = require('../config/connection.js');

var orm = {
        selectAll: function () {

        },

        insertOne: function () {

        },

        updateOne: function () {
          
        }
};

module.exports = orm;
