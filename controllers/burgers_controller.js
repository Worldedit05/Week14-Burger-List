// Create all the functions that will do all the routing in the app

var express = require('express');
var router = express.Router();
var burger = require('../models/burger.js');

module.exports = router;
